import { IonContent, IonHeader, IonPage, IonTitle, IonToolbar } from '@ionic/react';
import React from 'react';
import ChatComponen from '../components/Chat/Chat';
const Chat: React.FC = () => {
    return (
        <ChatComponen/>
    );
};

export default Chat;
